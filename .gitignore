# ============================================================
#  TTV Short Drama — 版本控制忽略规则
# ============================================================

# ---- 依赖与前端构建产物 ----
node_modules/
dist/
dist-ssr/
*.local

# ---- Rust / Tauri 构建产物 ----
src-tauri/target/
src-tauri/gen/schemas/

# ---- 运行期数据（设备凭据 / SQLite / WebView2 缓存，绝不入库）----
src-tauri/.app-data/
.app-data/
.webview-data/
*.sqlite3
*.sqlite3-shm
*.sqlite3-wal
short-drama-device.json

# ---- 工作目录与 AI / 编辑器工具产物（不属于项目内容）----
# `build.bat` / `start-dev-safe.bat` 把临时目录指向 .tmp；后三个是 AI 工具自己写的
# 工作区——`.zcode/plans/` 曾经因为漏了规则而被误提交（里面有一份含逆向计划的会话文档），
# 公开仓库里不应该出现它们。
.tmp/
.zcode/
.agents/
.workbuddy-ai/
.serena/

# ---- 随包分发的第三方运行期资源 ----
# 现已纳入版本控制，使仓库开箱即用。
# 其中 ffmpeg.exe 为 106,888,704 字节（101.94 MiB），**超过 GitHub 单文件
# 100 MiB 硬限制**，故交由 Git LFS 承载（见 .gitattributes）。
# 其余资源（嵌入式 Python 21.5MiB、worker 依赖 15.8MiB）单文件均远低于限制，
# 常规入库即可，无需占用 LFS 配额。
#
# 若你想让仓库只保留源码、不带这些可重新获取的第三方产物，取消下面三行的注释：
# src-tauri/resources/mpv/
# src-tauri/resources/python/
# src-tauri/resources/shortdrama-worker/site-packages/

# ---- Python ----
__pycache__/
# 只忽略编译缓存，**不要**用 *.py[cod]——
# 那个模式会连带忽略 *.pyd，而 .pyd 是 Python 的 C 扩展二进制
# （_ctypes.pyd、pycryptodome 的 _raw_aes.pyd 等），是运行必需品。
# 实测：该模式会漏掉 108 个 .pyd（6.32 MiB），导致安装包能装但运行失败。
*.pyc
*.pyo

# ---- 日志 ----
*.log
npm-debug.log*

# ---- 编辑器 / 操作系统 ----
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
.DS_Store
Thumbs.db
